import styled from '@emotion/styled'

// Color kit
const bgMainView = 'rgba(42, 66, 90, 1)'

// Styled-coponents
export const MainViewContainer = styled.div`
    width: 100%;
    padding: 4rem;
    color: white;
    background-color: ${bgMainView};
    position: relative;
`